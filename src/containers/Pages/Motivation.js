import React from 'react';
import cssClasses from '../../style/main.css';

const Motivation = (props) => {
    return(
        <div className={cssClasses.wrapper + " " + cssClasses.row}>
        <h1>Motivations</h1>
    </div>
    );
}

export default Motivation;